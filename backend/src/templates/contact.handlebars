<!DOCTYPE html>
<html dir="{{dir}}" lang="{{lang}}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{subject}}</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 2px solid #f97316;
    }
    .logo {
      max-width: 150px;
    }
    .content {
      padding: 20px 0;
    }
    .footer {
      text-align: center;
      padding: 20px 0;
      border-top: 1px solid #eee;
      font-size: 12px;
      color: #777;
    }
    .message-details {
      background-color: #f9f9f9;
      padding: 15px;
      border-radius: 5px;
      margin: 20px 0;
    }
    .message-field {
      margin-bottom: 10px;
    }
    .message-label {
      font-weight: bold;
      color: #f97316;
    }
  </style>
</head>
<body>
  <div class="header">
    <img src="https://evolve-fitness.com/logo.png" alt="Evolve Fitness" class="logo">
    <h1>{{title}}</h1>
  </div>
  
  <div class="content">
    <p>{{greeting}},</p>
    <p>{{intro}}</p>
    
    <div class="message-details">
      <div class="message-field">
        <span class="message-label">{{nameLabel}}:</span> {{name}}
      </div>
      <div class="message-field">
        <span class="message-label">{{emailLabel}}:</span> {{email}}
      </div>
      {{#if phone}}
      <div class="message-field">
        <span class="message-label">{{phoneLabel}}:</span> {{phone}}
      </div>
      {{/if}}
      <div class="message-field">
        <span class="message-label">{{subjectLabel}}:</span> {{subject}}
      </div>
      <div class="message-field">
        <span class="message-label">{{messageLabel}}:</span>
        <p>{{message}}</p>
      </div>
    </div>
    
    <p>{{outro}}</p>
  </div>
  
  <div class="footer">
    <p>© {{year}} Evolve Fitness. {{rightsText}}</p>
  </div>
</body>
</html>